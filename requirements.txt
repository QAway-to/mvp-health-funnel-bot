fastapi==0.104.1
uvicorn[standard]==0.24.0
python-telegram-bot==20.7
python-dotenv==1.0.0
aiohttp==3.9.1
rich==13.7.0
asyncpg==0.29.0

# Расшифровка голосовых для личного бота заметок (notes/).
# Тянет ctranslate2, av и onnxruntime — образ вырастает примерно на 300 МБ.
#
# ВЕРСИИ ЗАКРЕПЛЕНЫ ПО ЖИВОМУ, А НЕ ПО ВКУСУ. Сначала здесь стоял 1.0.3, и
# сборка упала: он делает `import requests`, а сам requests не объявляет —
# тот приезжал прицепом с huggingface-hub. Вышел huggingface-hub 1.x, отказался
# от requests, и цепочка порвалась.
#
# 1.2.1 от requests не зависит вовсе. Пара версий взята с машины, где эта же
# модель уже месяцами расшифровывает ролики проекта: собирать прод из того,
# что заведомо работает, дешевле, чем из самого свежего.
faster-whisper==1.2.1
huggingface-hub==0.35.3
