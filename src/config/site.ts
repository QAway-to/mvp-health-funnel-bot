/**
 * Единственное место, где живут данные, общие для всего сайта:
 * цены, ссылки оплаты, лид-магнит и юридические реквизиты.
 *
 * Значения в фигурных скобках — плейсхолдеры. Перед продом заменить их здесь,
 * все страницы подхватят автоматически. Полный список — в CHANGES.md.
 */

/** Куда уходит заявка с бесплатной ступени. Сейчас — бот в Telegram. */
export type LeadDestination = 'telegram_bot' | 'email' | 'both';

export const site = {
  /** Базовый URL для canonical и OG-тегов. Переопределяется PUBLIC_SITE_URL. */
  url: 'https://beg.example.com',

  /** Автор продукта. */
  author: {
    name: '{ИМЯ_СПИКЕРА}',
    yearsOfPractice: '{N}',
    studentsCount: '{N}',
    story:
      '{КОРОТКАЯ ИСТОРИЯ: сколько лет бегаете, какой поворот привёл к естественной технике, с кем работаете}',
  },

  /** Тарифы. */
  pricing: {
    premium: '{ЦЕНА}',
    base: '{ЦЕНА_БАЗА}',
    /** Во что обходится премиум в понятных величинах — для блока «Дорого». */
    comparison: '{СРАВНЕНИЕ}',
    /** Срок доступа к материалам. */
    accessPeriod: '{СРОК}',
    /** Бонус за оформление сегодня. */
    bonus: '{БОНУС}',
    /** Условие возврата. */
    guarantee: '{ГАРАНТИЯ}',
    /** Принимаемые способы оплаты. */
    paymentMethods: '{СПОСОБЫ_ОПЛАТЫ}',
    /** Количество уроков в гайде. */
    lessonsCount: '{N}',
    /** Формат: уроки, длительность, доступ. */
    format: '{ФОРМАТ: уроки, длительность, доступ}',
  },

  /**
   * Бесплатная ступень воронки. Форма на сайт не ставится: заявка уходит
   * в Telegram-бот, поэтому бэкенд и хранение персональных данных не нужны.
   */
  lead: {
    destination: 'telegram_bot' as LeadDestination,
    /** Ссылка на бота. Можно с deep-link параметром: https://t.me/bot?start=site */
    telegramUrl: '{ССЫЛКА_ТГ_БОТ}',
    /** Что человек получает бесплатно — обещание должно быть конкретным. */
    promise: '7-дневный старт: 5 минут в день, PDF и 3 коротких видео',
    /** Скидка на платный гайд в первые сутки после заявки. */
    firstDayDiscount: '{СКИДКА_24Ч}',
  },

  /** Бесплатный демо-урок. Пока ассетов нет — блок покажет плейсхолдер. */
  demo: {
    /** Ссылка на видео (YouTube/Vimeo/файл). Пусто — вместо плеера заглушка. */
    videoUrl: '',
    /** Постер для плеера, лежит в public/. */
    poster: '/img/barefoot-step.jpg',
    title: '{НАЗВАНИЕ_ДЕМО_УРОКА}',
    /** Текстовая расшифровка урока. */
    transcript: '',
  },

  /** Внешние ссылки. Открываются в том же окне. */
  links: {
    payBase: '{ССЫЛКА_ОПЛАТЫ_БАЗА}',
    payPremium: '{ССЫЛКА_ОПЛАТЫ_ПРЕМИУМ}',
    offer: '{ССЫЛКА_ОФЕРТА}',
    privacy: '{ССЫЛКА_ПОЛИТИКА}',
    contact: '{ССЫЛКА_КОНТАКТ}',
    /** Соцсети автора для страницы /avtor/. Пустые не отрисовываются. */
    telegramChannel: '',
    youtube: '',
    instagram: '',
  },

  /** Футер. */
  legal: {
    year: '{ГОД}',
    entity: '{ЮРИДИЧЕСКИЕ_ДАННЫЕ}',
  },
} as const;

/** Маршруты сайта в одном месте, чтобы ссылки не разъезжались. */
export const routes = {
  home: '/',
  bezBoli: '/bez-boli/',
  sila: '/sila/',
  start: '/start/',
  startThanks: '/start/spasibo/',
  demo: '/demo/',
  course: '/kurs/',
  author: '/avtor/',
} as const;

export type Site = typeof site;
