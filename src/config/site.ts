/**
 * Единственное место, где живут данные, общие для всего сайта:
 * цена подписки, ссылка оплаты, лид-магнит и юридические реквизиты.
 *
 * Незаполненное значение — это пустая строка, а не «{ПЛЕЙСХОЛДЕР}»: скобки
 * доезжали до прода и попадали человеку на глаза. Теперь пустое поле означает
 * «блок не рисуем», и страница остаётся целой. Что именно ждём от заказчика —
 * в CHANGES.md.
 */

/** Куда уходит заявка с бесплатной ступени. Сейчас — бот в Telegram. */
export type LeadDestination = 'telegram_bot' | 'email' | 'both';

export const site = {
  /** Базовый URL для canonical и OG-тегов. Переопределяется PUBLIC_SITE_URL. */
  url: 'https://beg.example.com',

  /** Имя проекта: шапка хаба, футер, юридическая строка. */
  brand: 'Федерация здоровья',

  /**
   * Автор продукта. Пустые поля не рисуются: блок автора сжимается
   * до того, что известно, вместо строки «{ИМЯ}, {N} лет».
   */
  author: {
    name: 'Богдан Боровский',
    /** Например '12'. Пусто — метрика и подзаголовок без неё. */
    yearsOfPractice: '',
    /** Например '400'. Пусто — метрика не показывается. */
    studentsCount: '',
    /** Короткая история: сколько лет практики, что привело, с кем работает. */
    story: '',
  },

  /**
   * Единая подписка на все направления Федерации.
   *
   * Продукт один: доступ ко всем тренингам сразу, а не отдельная покупка
   * каждого. Поэтому здесь одна цена, а не сетка тарифов: направление
   * добавляется в реестр и сразу входит в ту же подписку.
   *
   * Пустая цена и пустая ссылка — это рабочее состояние, а не поломка:
   * компонент подписки показывает бесплатную ступень вместо кнопки оплаты.
   * См. src/lib/offer.ts.
   */
  subscription: {
    /** Например '990 ₽'. Пусто — блок цены не показывает сумму. */
    price: '',
    /** Период списания: подставляется рядом с ценой. */
    period: 'в месяц',
    /** Ссылка на оплату LavaTop. Пусто — кнопка ведёт на бесплатную ступень. */
    payUrl: '',
    /**
     * Имя query-параметра, которым чекаут принимает метку источника.
     *
     * У бесплатной ступени метка направления едет в адресе (`/start/?s=son`) и
     * доезжает до бота. На платном шаге терять её не хочется: иначе в день
     * включения оплаты пропадает ответ на вопрос, какое направление окупается.
     * Но чужая страница оплаты принимает только те параметры, которые сама
     * объявила, поэтому имя берётся отсюда: пусто — ничего не добавляем.
     * Заполнить, когда сверим с документацией LavaTop.
     */
    trackingParam: '',
    /** Строка под кнопкой: условия отмены. Пусто — строка не рисуется. */
    terms: 'Отменить можно в любой момент',
  },

  /**
   * Бесплатная ступень воронки. Форма на сайт не ставится: заявка уходит
   * в Telegram-бот, поэтому бэкенд и хранение персональных данных не нужны.
   */
  lead: {
    destination: 'telegram_bot' as LeadDestination,
    /** Ссылка на бота. Можно с deep-link параметром: https://t.me/bot?start=site */
    telegramUrl: 'https://t.me/test_notify35_bot',
    /** Что человек получает бесплатно — обещание должно быть конкретным. */
    promise: '7-дневный старт: 5 минут в день, PDF и 3 коротких видео',
    /** Скидка на подписку в первые сутки после заявки. Пусто — не обещаем. */
    firstDayDiscount: '',
  },

  /** Бесплатный демо-урок. Пока ассетов нет — блок покажет плейсхолдер. */
  demo: {
    /**
     * Ссылка на ролик. Понимает:
     *   TikTok  — tiktok.com/@автор/video/123…  (нужен ПОЛНЫЙ адрес, не vm.tiktok.com)
     *   YouTube — watch?v=, youtu.be, /shorts/, /embed/
     *   файл    — путь в public/, например '/video/demo.mp4'
     *
     * Что именно вставлено, определяет src/lib/video.ts.
     * Пусто — вместо плеера заглушка с подсказкой.
     */
    videoUrl: 'https://www.tiktok.com/@bogdan88899/video/7653562675468717333',
    /** Постер для плеера, лежит в public/. */
    poster: '/img/barefoot-step.jpg',
    title: '',
    /** Текстовая расшифровка урока. */
    transcript: '',
  },

  /**
   * Внешние ссылки. Открываются в том же окне.
   * Пустые значения не рисуются: пустая ссылка лучше ссылки в никуда.
   */
  links: {
    offer: '',
    privacy: '',
    contact: '',
    /** Соцсети автора для страницы /avtor/. Пустые не отрисовываются. */
    telegramChannel: '',
    youtube: '',
    instagram: '',
  },

  /** Футер. */
  legal: {
    year: '2026',
    entity: 'Все права защищены',
  },
} as const;

/** Маршруты сайта в одном месте, чтобы ссылки не разъезжались. */
export const routes = {
  home: '/',
  beg: '/beg/',
  komfort: '/komfort/',
  sila: '/sila/',
  son: '/son/',
  zaryadka: '/zaryadka/',
  samomassazh: '/samomassazh/',
  massazh: '/massazh/',
  start: '/start/',
  startThanks: '/start/spasibo/',
  demo: '/demo/',
  course: '/kurs/',
  author: '/avtor/',
} as const;

export type Site = typeof site;
