/**
 * Единственное место, где живут данные, общие для обоих лендингов:
 * цены, сроки, ссылки на оплату и юридические реквизиты.
 *
 * Значения в фигурных скобках — плейсхолдеры из дизайн-хендоффа.
 * Перед продом заменить их здесь; оба лендинга подхватят автоматически.
 */

export const site = {
  /** Базовый URL для canonical и OG-тегов. */
  url: 'https://beg.example.com',

  /** Автор продукта. */
  author: {
    name: '{ИМЯ_СПИКЕРА}',
    yearsOfPractice: '{N}',
    studentsCount: '{N}',
    story:
      '{КОРОТКАЯ ИСТОРИЯ: сколько лет бегаете, какой поворот привёл к естественной технике, с кем работаете}',
  },

  /** Тарифы. */
  pricing: {
    premium: '{ЦЕНА}',
    base: '{ЦЕНА_БАЗА}',
    /** Во что обходится премиум в понятных величинах — для блока «Дорого». */
    comparison: '{СРАВНЕНИЕ}',
    /** Срок доступа к материалам. */
    accessPeriod: '{СРОК}',
    /** Бонус за оформление сегодня. */
    bonus: '{БОНУС}',
    /** Условие возврата. */
    guarantee: '{ГАРАНТИЯ}',
    /** Принимаемые способы оплаты. */
    paymentMethods: '{СПОСОБЫ_ОПЛАТЫ}',
    /** Количество уроков в гайде. */
    lessonsCount: '{N}',
    /** Формат: уроки, длительность, доступ. */
    format: '{ФОРМАТ: уроки, длительность, доступ}',
  },

  /** Внешние ссылки. Открываются в том же окне. */
  links: {
    payBase: '{ССЫЛКА_ОПЛАТЫ_БАЗА}',
    payPremium: '{ССЫЛКА_ОПЛАТЫ_ПРЕМИУМ}',
    offer: '{ССЫЛКА_ОФЕРТА}',
    privacy: '{ССЫЛКА_ПОЛИТИКА}',
    contact: '{ССЫЛКА_КОНТАКТ}',
  },

  /** Футер. */
  legal: {
    year: '{ГОД}',
    entity: '{ЮРИДИЧЕСКИЕ_ДАННЫЕ}',
  },
} as const;

export type Site = typeof site;
