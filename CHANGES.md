# Перестройка воронки — статус работ

Работа остановлена по вашей команде после этапа (b) плюс две страницы, без которых
сборка ушла бы в прод с битыми ссылками. Сайт в рабочем состоянии, деплой не сломан.

---

## Что сделано

### Этап (a) — контент в данные

| Файл | Что внутри |
| --- | --- |
| `src/config/site.ts` | Цены, ссылки оплаты, лид-магнит (Telegram), демо-урок, соцсети, реквизиты. Плюс карта маршрутов `routes`. |
| `src/content/course.ts` | Состав продукта. Блоки берутся из `landing-a`, не дублируются. Названия уроков ждут заказчика. |
| `src/content/testimonials.ts` | Схема отзывов с сегментами `pain` / `gym` / `both`. Список пуст — выдуманных отзывов нет. |
| `src/content/home.ts` | Контент продающей главной. Причины, техника, автор, FAQ и тарифы **импортируются** из `landing-a`. |
| `src/content/cta.ts` | Подписи кнопок в одном месте. |

Ключевое решение под требование «не переписывай»: новые страницы **импортируют**
существующие тексты, а не копируют их. Формулировки физически не могут разойтись,
и правка в одном месте расходится по всему сайту.

### Этап (b) — система CTA и sticky-панель

Главная проблема воронки закрыта: **продажный CTA больше не появляется в шапке
и в первом экране**.

- `src/components/Cta.astro` — единственная кнопка сайта, три типа:
  `primary` (бесплатная ступень), `secondary` (узнать больше), `sales` (покупка).
  Правило «sales не выше 60% глубины» описано на компоненте и проверяется одной командой:
  `grep -rn 'kind="sales"' src/`
- Шапка: было «Получить доступ» → `#price`, стало «Первый шаг бесплатно» → `/start/`.
- Герой: цена убрана, стоят «Забрать первый шаг бесплатно» и «Посмотреть демо-урок».
- `src/components/StickyCta.astro` — панель внизу экрана на мобильном: до 60% глубины
  бесплатный CTA, после — с ценой. Над первым экраном скрыта, чтобы не дублировать
  кнопку в герое.
- `src/scripts/track.ts` — обёртка `track(event, payload)` с закрытым списком событий.

**Попутно найден и исправлен баг вёрстки:** scoped-правила кнопок тарифов полностью
выпадали из сборки, потому что кнопку рендерит компонент `Cta` и она не попадала
в scope родителя. Тариф «База» отрисовывался яркой кнопкой, неотличимой от «Премиум».

### Две страницы вне плана этапов

Созданы вынужденно: шапка и герой уже ссылались на них.

- `/start/` — бесплатная ступень. Формы нет, заявка уходит в Telegram-бот, поэтому
  бэкенд не нужен и персональные данные не хранятся. Пока ссылка на бота не задана,
  на странице стоит явное указание, что вписать.
- `/demo/` — бесплатный демо-урок. Разбор техники взят из существующего контента.
  Пока видео нет — заглушка вместо плеера, выдуманного контента нет.

---

## Что НЕ сделано

| Этап | Статус |
| --- | --- |
| (c) Перестройка главной в 10 секций | **не начат** — `/` остаётся старой развилкой из двух карточек. `src/content/home.ts` готов, но ещё не подключён |
| (d) Перестройка `/komfort/` и `/sila/` | **не начат** — порядок секций прежний, FAQ по-прежнему стоит ПОСЛЕ цены |
| (e) Страницы `/start/spasibo/`, `/kurs/`, `/avtor/` | **не начаты** |
| (f) Микро-квиз | **не начат** |
| (g) Аналитика и SEO | **частично** — обёртка `track()` есть, события подключены только у sticky-панели. Нет `Product` schema, скролл-депта 25/50/75/100, событий `view_hero`, `quiz_*`, `lead_submitted`, `demo_played`, `pricing_viewed` |
| Отзывы | схема есть, компонент не написан |
| Блок «что именно получаешь» | данные есть (`course.ts`), компонент не написан |
| Гарантия отдельным блоком | не сделано, остаётся строкой в тарифах |
| webp/avif | не сделано, картинки JPEG (1.2 МБ) |

Заметьте: `src/content/home.ts` сейчас **никем не используется** — он подключится
на этапе (c). Это не мусор, но и не рабочий код.

---

## Нужно от вас (без этого блоки стоят пустыми)

1. **Ссылка на Telegram-бота** → `site.lead.telegramUrl`. Без неё `/start/` показывает
   заглушку вместо кнопки. Это единственное, что мешает бесплатной ступени работать.
2. **Видео демо-урока** и расшифровка → `site.demo.videoUrl`, `site.demo.transcript`.
3. **Отзывы** — минимум 3, в формате «было → стало», с фото и сегментом
   (`pain` для `/komfort/`, `gym` для `/sila/`) → `src/content/testimonials.ts`.
4. **Названия уроков** по четырём блокам → `src/content/course.ts`, поле `lessons`.
5. **Цены и условия**: `{ЦЕНА}`, `{ЦЕНА_БАЗА}`, `{ГАРАНТИЯ}`, `{СРОК}`, `{БОНУС}`,
   `{СПОСОБЫ_ОПЛАТЫ}`, `{СКИДКА_24Ч}`, ссылки оплаты.
6. **Автор**: имя, годы практики, число учеников, история, **реальное фото**
   (сейчас `mountain.jpg` — сток).
7. **Юридическое**: реквизиты, ссылки на оферту и политику.

Ничего из этого не выдумано и не заполнено «правдоподобно» — везде плейсхолдеры.

---

## Чеклист ручной проверки

- [ ] Шапка: кнопка «Первый шаг бесплатно», продажной кнопки нет
- [ ] Первый экран `/komfort/` и `/sila/`: цены нет
- [ ] Мобильный 375px: sticky скрыта на первом экране, появляется при скролле,
      на 80% глубины меняется на кнопку с ценой
- [ ] Тарифы: «База» — контурная кнопка, «Премиум» — яркая
- [ ] `/start/` и `/demo/` открываются, шапка и футер на месте
- [ ] 404 на несуществующем адресе

Автоматически проверено: `astro check` — 0 ошибок; битых внутренних ссылок нет;
6 страниц в сборке; горизонтального оверфлоу на 375px нет.

---

## Деплой

Не изменился: `npm install && npm run build` + `npm start`, Render Web Service.

---

## Правки от 2026-08-20

### Формулировки про боль убраны

Медицинская лексика заменена на риторику дискомфорта во всём контенте:

| Было | Стало |
| --- | --- |
| «Бег без боли» (бренд) | «Бег без дискомфорта» |
| «Колени болят не от бега» | «Дискомфорт в коленях — не от бега» |
| «Боль появляется от четырёх привычек» | «Дискомфорт появляется от четырёх привычек» |
| «Ноют колени и стопы» | «Тяжесть в коленях и стопах» |
| «Появилась боль — шаг назад» | «Появился дискомфорт — шаг назад» |
| «У меня больные колени» | «У меня чувствительные колени» |
| «Протокол на „что-то заболело“» | «Протокол на „что-то беспокоит“» |
| «без боли на следующий день» | «без тяжести на следующий день» |

Отдельно: в ответе на возражение сохранена рекомендация обратиться к специалисту —
переформулирована как «если колени беспокоят всерьёз», без слова «боль». Убирать её
целиком было бы неправильно.

**Маршрут переименован:** `/bez-boli/` → `/bez-diskomforta/`, иначе слово оставалось бы
в адресной строке и в расшаренных ссылках. Со старого адреса стоит 301 в `server.mjs`,
так что уже разошедшиеся ссылки продолжают работать.

### Тема стала светлой

Чёрный фон заменён на бежевый, стиль осветлён.

- Палитра описана токенами в `src/styles/global.css`. **Все 27 пар «текст на фоне»
  проверены на контраст WCAG AA скриптом**, а не на глаз.
- Введён отдельный токен `--accent-text` (#8A5A08). Причина: жёлтый `--accent`
  (#E8B23C) на бежевом даёт контраст ~1.6:1 и как текст нечитаем. Теперь `--accent` —
  только заливка (кнопки, линии, маркеры), `--accent-text` — текст.
- Вуали поверх фотографий были рассчитаны на тёмную тему и инвертированы; сами фото
  больше не затемняются, а чуть высветляются.
- Усилены две вуали, где текст ложился на фото: подпись под кнопкой в финальном CTA
  и заголовки карточек развилки.
- Favicon и `theme-color` переведены на светлую тему.

Проверено: `astro check` чисто, битых ссылок нет, скриншоты десктопа и мобильного
просмотрены глазами.

---

## Правки от 2026-08-20 (вторая итерация)

### Риторика от утверждения, а не от отрицания

«Бег без дискомфорта» — это всё ещё отрицание. Позиционирование перестроено
на положительное:

| Было | Стало |
| --- | --- |
| «Бег без дискомфорта» (бренд) | «Комфортный бег» |
| «Дискомфорт в коленях — не от бега» | «Комфортный бег начинается с техники» |
| «Дискомфорт появляется от четырёх привычек» | «Комфорт складывается из четырёх привычек» |
| «Хочу бегать без дискомфорта в коленях» | «Хочу, чтобы бег был комфортным для коленей» |
| «без тяжести на следующий день» | «и наутро ноги свежие» |

Маршрут снова переименован: `/bez-diskomforta/` → **`/komfort/`**, потому что
«без» в адресе — то же отрицание. Редиректы 301 стоят с обоих прежних слагов
(`/bez-boli/` и `/bez-diskomforta/`), так что ни одна разошедшаяся ссылка не умерла.

### Заголовки в одну строку

Причина переносов была не в длине текста, а в `max-width`, заданном в `ch`:
он обрезал строку заметно раньше контейнера. Например, «Четыре блока плюс карта
недели» упиралось в 854px при доступных 1152px — отсюда одинокое слово на второй
строке.

Что сделано:

- сняты все `ch`-ограничения с заголовков секций;
- предельный кегль подобран **замером**, а не на глаз: скрипт считает натуральную
  ширину каждого заголовка и требуемый размер. `.h2` → максимум 46px, герой → 60px,
  финальный CTA → 64px;
- добавлен `text-wrap: balance` на все заголовки: там, где перенос неизбежен
  (мобильный, узкие колонки), строки делятся поровну, без висящего слова.

Результат на 1440px: все заголовки секций в одну строку. Переносятся только два —
заголовок блока «как это работает» и заголовок автора, оба стоят в колонке шириной
536px рядом с фотографией, где одна строка невозможна при читаемом кегле. У автора
это ещё и плейсхолдер `{ИМЯ_СПИКЕРА}, {N} лет` — с реальным именем станет короче.

### Сегменты — по желаниям, без анатомии

Упоминания коленей убраны полностью (проверено `grep -i "колен"` — ноль совпадений).
Сегменты теперь описывают желаемый результат:

| | Было | Стало |
| --- | --- | --- |
| Вариант 1 | «Комфортный бег» / «комфортным для коленей» | «Комфортный бег» → **«Хочу бегать легко и в удовольствие»** |
| Вариант 2 | «Бег как сила» / «Уже тренируюсь, но бег не идёт» | **«Сила и выносливость»** → **«Хочу выносливость, не потеряв силу»** |

Где колено называлось по делу — в описании техники — формулировки переписаны
без потери смысла:

| Было | Стало |
| --- | --- |
| «Мягкое колено» (принцип техники) | «Мягкая амортизация» |
| «Слегка согнуто на всей опоре» | «Нога слегка согнута на всей опоре» |
| «удар уходит вверх: колено, бедро, поясница» | «удар уходит вверх по всей ноге в поясницу» |
| «торможение и удар в колено» | «торможение и жёсткий удар вверх» |
| «ноют колени, падает тяга» | «ноги забиты, падает тяга» |
| «Колени, голени, просевшая тяга» | «Забитые ноги и просевшая тяга» |
| «У меня чувствительные колени» (возражение) | «Я давно не в форме» |

Рекомендация обратиться к специалисту в ответе сохранена — переформулирована
как «если что-то беспокоит всерьёз».

Сегменты отзывов переименованы под новую логику: `pain` / `gym` → `comfort` / `strength`.

Заодно: `text-wrap: balance` распространён на заголовки карточек — они не heading,
и глобальное правило их не покрывало, из-за чего предлог повисал в конце строки.

### Демо-урок: подключение видео

`site.demo.videoUrl` теперь принимает **ссылку на YouTube в любом виде** —
`watch?v=`, `youtu.be/`, `/shorts/`, `/embed/`, `/live/`, с параметрами и без —
либо путь к файлу в `public/` (`/video/demo.mp4`). Что вставлено, определяет
`src/lib/video.ts`; проверено на 15 формах ссылок, включая мусорные.

Было расхождение: комментарий в конфиге обещал YouTube, а код рендерил нативный
`<video src>`, который умеет только прямой файл. Ссылка на YouTube в нём
не заработала бы, причём молча. Исправлено.

**Плеер грузится по клику.** До нажатия на странице только постер и кнопка;
запросов к YouTube — ноль (проверено перехватом сети: единственный внешний
запрос до клика — Google Fonts). Встроенный сразу iframe тянет ~700 КБ
на каждой загрузке страницы и не оставил бы шанса уложиться в LCP < 2.5s.

Мелочи: адрес плеера — `youtube-nocookie.com`, без трекинг-кук до воспроизведения;
`rel=0` убирает чужие ролики в конце; Shorts определяются автоматически
и показываются в 9:16, а не режутся из 16:9. Событие `demo_played` уходит
при старте и для YouTube, и для файла.

**TikTok не поддержан осознанно.** У него нет обычного iframe-адреса:
официальное встраивание требует `blockquote` плюс сторонний скрипт `embed.js`,
формат вертикальный, а в конце плеера показываются чужие ролики. Это отдельная
работа, а не «ещё один case» в парсере — при необходимости сделаю отдельно.

### TikTok поддержан

Ролики у заказчика лежат в TikTok, поэтому добавлена его поддержка. Моё прежнее
утверждение, что у TikTok нет iframe-адреса, **было неверным**: я смотрел раздел
про blockquote-встраивание, а официальный iframe-плеер описан отдельно —
`tiktok.com/player/v1/{id}`. Сторонний скрипт `embed.js` не нужен.

`site.demo.videoUrl` теперь понимает:

| Что вставили | Результат |
| --- | --- |
| `tiktok.com/@автор/video/123…` | плеер TikTok, 9:16 |
| `tiktok.com/@автор/photo/123…` | плеер TikTok, 9:16 |
| `youtube.com/watch?v=`, `youtu.be/`, `/shorts/` | плеер YouTube |
| `/video/demo.mp4` | нативный `<video>` |
| `vm.tiktok.com/…`, `tiktok.com/t/…` | подсказка: нужна полная ссылка |
| пусто или мусор | заглушка |

**Важно про короткие ссылки.** Кнопка «Поделиться» в TikTok даёт адрес вида
`vm.tiktok.com/ZMxxxx/` — в нём нет ID ролика, и определить его без сетевого
запроса невозможно. Такая ссылка не молчит, а выводит на странице подсказку:
откройте видео на сайте и скопируйте полный адрес. Нужна ссылка с `/video/`.

Параметры плеера: `rel=0` — в конце показываются ролики автора, а не случайные
чужие; описание и музыка скрыты. Событие `demo_played` уходит с указанием
платформы.

**Найден и исправлен баг вёрстки:** вертикальный плеер схлопывался в полоску
высотой пару пикселей. Причина та же, что была у контейнера: `margin-inline: auto`
отключает растягивание флекс-элемента, а все дети внутри абсолютно спозиционированы,
поэтому при одном лишь `max-width` ширина вычислялась в ноль. Ширина задана явно.

Проверено в браузере: до клика запросов к TikTok ноль, после клика создаётся
iframe 420×747, событие уходит.

---

## Правки от 21 августа 2026

### Лендинг сшит с Telegram-ботом

Сегмент человека известен уже на лендинге: он пришёл либо за комфортом, либо за
выносливостью. Раньше эта метка терялась на первом же переходе — все кнопки вели
на голый `/start/`, а оттуда в бота уходила одна безымянная ссылка.

Теперь метка едет с человеком до конца:

| Откуда нажали | Адрес бесплатной ступени | Что уезжает в бота |
| --- | --- | --- |
| `/komfort/` — шапка, первый экран, sticky | `/start/?s=komfort` | `?start=komfort` |
| `/sila/` — то же | `/start/?s=sila` | `?start=sila` |
| `/demo/` | `/start/?s=demo` | `?start=demo` |
| прямой заход на `/start/` | `/start/` | `?start=site` |

Бот кладёт это в поле `source` и пишет в аналитику — значит на вопрос «какой
сегмент окупается» появляется ответ, а первый вопрос в диалоге не тратится на то,
что и так известно.

Сегмент берётся из уже существующего поля `slug` в контенте лендинга — новых
данных заводить не пришлось. Страница `/start/` статическая и раздаётся всем одна,
поэтому подстановку делает маленький скрипт при загрузке. Значение проверяется по
закрытому списку (`komfort`, `sila`, `demo`, `home`): в чужой домен не уезжает
ничего, чего мы сами не заводили — `?s=hack:evil` молча превращается в `site`.

### Гарантия возврата убрана

Удалена из семи мест: строки доверия на обоих первых экранах, отдельная строка под
тарифами, ответ на возражение «дорого», подписи под финальными кнопками и строка на
`/demo/`. Заодно убраны поле `guarantee` из конфига и из типа `Pricing` — иначе
осталось бы требование заполнять то, чего больше нигде нет.

### Демо-плеер: лоадер и запасной выход

Между кликом и первым кадром страница больше не молчит: крутилка с подписью
«Загружаем плеер…» поверх притемнённого постера. Снимается по сигналу
`onPlayerReady` от плеера, а не по `load` у iframe — `load` означает лишь, что
загрузился документ плеера, кадра за ним ещё нет.

Если через полторы секунды кадр нулевой высоты (так выглядит работа блокировщика
рекламы) или через пятнадцать секунд видео так и не пошло — появляется полоса
«Ролик не запускается? Открыть в TikTok». Полоса стоит по низу, а не полотном
поверх: если видео всё-таки играет, перекрытие перехватило бы клик по паузе.

### Три тарифа вместо двух

**$9 База · $20 Премиум · $100 Сопровождение.** Цены в долларах: аудитория живёт
в разных странах, и рубль на лендинге отсекал бы часть людей ещё до кнопки.

Тарифы стали списком вместо двух именованных полей — четвёртый добавится одной
записью. Метка «РЕКОМЕНДУЮ» теперь не украшение: помеченный тариф подставляется
в мобильную панель и в сквозные подписи кнопок. У каждого тарифа появился
латинский `id` — он уходит в аналитику как `placement`, иначе в отчёте были бы
`price_база` и `price_сопровождение`.

Состав ступени за $100 — **черновик, который нужно подтвердить**: восемь недель,
разбор видео каждую неделю, правка плана под темп, личный чат с ответом в течение
дня, ограниченный набор. Это обещания, которые придётся выполнять руками, и объём
работы у них немаленький.

### Тарифы одной высоты

Карточки выровнены структурно, а не подгонкой: у сетки было `align-items: start`
(мой недосмотр при переходе на три тарифа) — вернул растягивание, кнопка внутри
и раньше прижималась к низу. Абзацу под ценой задано место под три строки, иначе
списки начинались на разной высоте: снаружи карточки ровные, а внутри съехавшие.

Пункты переписаны так, чтобы влезать в одну строку при ширине карточки 368px —
предел около 31 знака:

| Было | Стало |
| --- | --- |
| Дозировки по поверхностям и погоде | Дозировки по грунту и погоде |
| Разбор вашего видео каждую неделю — 8 разборов | Восемь разборов вашего видео |
| План правится под ваш темп, а не под средний | План правится под ваш темп |
| Набор ограничен — столько людей я успеваю вести | Набор ограничен |
| Встройка бега в твой недельный план | Бег внутри твоей недели |

Оговорка про ограниченный набор переехала в абзац («Веду немного людей
одновременно»): в пункт она не помещалась, а без объяснения строка звучит как
искусственный дефицит.

Проверено замером в браузере на обеих страницах: высоты 488/488/488, ни одного
переноса в пунктах, цены, списки и кнопки — на одной линии.



---

## Правки от 26 августа 2026 — мини-сайт Федерации

Сайт перестал быть двумя лендингами одного гайда и стал мини-сайтом всех
направлений Федерации здоровья — это пункт П14 плана, который до сих пор стоял
на нуле. Заодно закрыта дыра, из-за которой запускаться было нельзя.

### Кнопки покупки больше не ведут в 404

Было: в вёрстку уезжал литерал `{ССЫЛКА_ОПЛАТЫ_ПРЕМИУМ}`, и клик по цене открывал
ненайденную страницу. На проде так работали все пять продажных кнопок.

Стало: решение принимает `src/lib/offer.ts`. Пока ссылка оплаты не заведена,
продажная кнопка становится бесплатной ступенью и ведёт в бота, а мобильная
панель не подменяет призыв на цену, которой нет. Ни одного `{ПЛЕЙСХОЛДЕРА}`
в собранных страницах не осталось — проверено по всему `dist/`.

Заодно: пустые поля конфига теперь пустые строки, а не текст в фигурных скобках.
Незаполненный блок не рисуется вместо того, чтобы показывать человеку `{N} лет`.
Юридические ссылки в футере рисуются только заполненные: ссылка «Оферта»,
ведущая на текущую же страницу, читается как поломка сайта.

### Три тарифа заменены одной подпиской

Продукт один — доступ ко всем направлениям сразу. Поэтому вместо сетки
$9/$20/$100 одна карточка `Subscription.astro`, а состав подписки собирается
из реестра направлений: открылось новое — строка появилась сама.

Цена и ссылка оплаты ждут заказчика, но пустыми они сайт не ломают.

### Реестр направлений и главная

`src/content/directions.ts` — единственный источник правды о том, какие
направления существуют и какие открыты. Из него собираются главная, блок
подписки и список «готовятся».

- `/` — хаб: 5 открытых направлений карточками, 6 готовящихся списком.
  Готовящиеся намеренно не ссылки: вести некуда.
- `/beg/` — развилка «комфорт / сила», переехала с корня. Метка сегмента
  довозится до бота как раньше.

### Четыре новых направления

`/son/`, `/zaryadka/`, `/samomassazh/`, `/massazh/` — из материалов заказчика
(папка «feder»). Тексты написаны по слайдам, а не выдуманы; фотографии взяты
из тех же презентаций.

Что важно знать про источники:

| Материал | Что было | Что с ним сделано |
| --- | --- | --- |
| «Здоровий сон», 13 страниц | Украинский, с текстовым слоем | Переведён на русский, лёг в основу `/son/` — самый полный из четырёх |
| «САМОМАСАЖ», 7 страниц + 7 слайдов | Украинский и русский, содержание общее | Русская версия в основе `/samomassazh/` |
| «Массаж», 15 слайдов | Русский, картинками | В основе `/massazh/` |
| «Зарядка», 13 слайдов | Русский, картинками | В основе `/zaryadka/` |

**Медицинские обещания из исходников не перенесены.** В презентациях массаж
«лечит заболевания» и «залечивает давние травмы», самомассаж «повышает
иммунитет», недосып ведёт к списку диагнозов. На лендинге такое обещать нельзя,
поэтому формулировки заменены на проверяемые: снять напряжение, засыпать
вовремя, восстанавливаться после нагрузки. Противопоказания, наоборот,
сохранены — это как раз то, что человеку нужно знать.

**У зарядки материал самый слабый.** Её слайды — это заголовки и открытые
вопросы («зачем и как это делать?»), содержательного ответа почти нет: только
список из четырнадцати шагов и состав первого приёма пищи. Лендинг обещает
порядок утра и перечисляет, на какие вопросы отвечает направление, — обещать
ответы, которых у нас нет, было бы выдумкой. Как только появятся уроки,
страница дополнится без переделки.

### Секции стали необязательными

`LandingContent` требует теперь только первый экран, вопросы и финальный призыв.
У направления материала меньше, чем у бегового лендинга: `/massazh/` идёт без
плана на неделю, все четыре — без блока автора. Пропущенная секция не рисуется
и не сдвигает чередование фонов.

### Вопросы переехали перед ценой

Требование этапа (d). Возражение «дорого» разбирается в вопросах, и разбирать
его после того, как человек увидел сумму, поздно. Порядок теперь одинаков на
всех страницах: … → автор → вопросы → подписка → финальный призыв.

### Проверено

- `astro check` — 0 ошибок, 11 страниц в сборке;
- ни одного `{ПЛЕЙСХОЛДЕРА}` и ни одной битой внутренней ссылки в `dist/`;
- горизонтального оверфлоу на 375px нет ни на одной из восьми страниц —
  замером в браузере. Попутно найден и исправлен настоящий оверфлоу шапки:
  длинное имя «Федерация здоровья» рядом с кнопкой выталкивало её за экран,
  потому что колонка grid-а не сжимается ниже min-content;
- мобильная панель на странице без оплаты показывает одну кнопку и не
  переключается;
- метка сегмента доезжает до бота: `/son/` → `/start/?s=son`.

---

## Правки от 26 августа 2026 — отзывы и три уровня подписки

### Три уровня вместо одной цены

Цены прежние — **$9 База · $20 Премиум · $100 Сопровождение**, — но теперь это
уровни **одной подписки на все направления**, а не отдельные продукты.
Принципиально: уровни отличаются **обратной связью, а не объёмом материалов**.
Все направления открыты на любом уровне, иначе «База» читалась бы как урезанный
продукт, а не как способ идти самому.

Цена и ссылка оплаты каждого уровня лежат в `site.subscription.tiers`, копирайт —
в `src/content/subscription.ts`, склеиваются по `id`. Если контент опишет уровень,
которого нет в конфиге, сборка упадёт: карточка без цены в проде хуже, чем
непрошедшая сборка.

**Пока оплата не подключена, кнопок на карточках нет вовсе.** Три одинаковых
«Первый шаг бесплатно» под тремя разными ценами читались как ошибка вёрстки.
Вместо них — один общий призыв под сеткой и честная строка о том, что оплата
подключается. Появится ссылка у уровня — кнопка вернётся на его карточку сама.

### Блок отзывов

`src/components/Testimonials.astro` — формат «было → стало», карточка с именем,
контекстом и фото. Без фото рисуется инициал на плашке акцента: настоящее имя
без силуэта-заглушки читается лучше.

Секция стоит **перед вопросами и ценой**: чужой результат отвечает на «а
сработает ли у меня» до того, как этот вопрос станет возражением.

Отзывы отбираются по сегменту страницы — на странице сна не место истории про
бег. Сегмент это либо слаг направления, либо `all` (виден везде, включая
главную). Опечатка в слаге роняет сборку: иначе отзыв молча не показался бы
нигде. Беговые лендинги ведут два своих сегмента — `komfort` и `sila`.

**Настоящих отзывов у нас нет, поэтому список пуст и секция не рисуется ни на
одной странице.** Выдуманные отзывы не годятся: это ровно то место, где вымысел
дороже всего обходится. Вёрстка проверена на временных данных и они удалены —
в репозитории их нет.

Попутно: пустая секция больше не съедает очередь фонов. Раньше пропущенный блок
сдвинул бы чередование и два одинаковых фона встали бы подряд.

### Проверено

- `astro check` — 0 ошибок, 11 страниц;
- порядок секций: … → автор → отзывы → вопросы → подписка → финальный призыв;
- три карточки уровней одной высоты (401px), цены и метка «РЕКОМЕНДУЮ» на месте;
- горизонтального оверфлоу на 375px нет ни на одной из десяти страниц.

---

## Правки от 26 августа 2026 — три дефекта и аналитика

### Canonical и OG вели на несуществующий домен

`beg.example.com` стоял заглушкой в `astro.config.mjs` и уезжал в прод: каждая
страница сообщала поисковику, что настоящая версия живёт на другом домене, а
мессенджер не мог забрать картинку превью. Теперь по умолчанию живой адрес на
Render. Появится свой домен — задать `PUBLIC_SITE_URL` в переменных окружения,
код не трогать.

### Демо-урок вёл не туда

Вторая кнопка первого экрана всегда вела на демо-урок, а ролик у нас один и он
про технику бега. Человек со страницы сна попадал на разбор постановки стопы.
Кнопка теперь берётся из контента: у беговых лендингов это демо, у остальных
направлений — их собственный состав. На главной вместо демо — переход к списку
направлений.

### Бесплатная ступень обещала бег всем

`/start/` дословно обещала «7-дневный старт по естественной технике бега» — в
том числе тем, кто пришёл со сна или массажа, а таких переходов теперь четыре
страницы из пяти. Страница переписана под то, что бот делает на самом деле:
чек-лист, ответы по материалам и короткий разбор.

**Отдельно добавлена честная строка про границы:** база знаний бота собрана по
бегу, по остальным направлениям он отвечает тем, что есть. Обещать разбор сна
там, где его нет, значило бы обманывать человека на первом же шаге. Это не
косметика, а то, что нужно закрыть до рекламы: см. список ниже.

### Аналитика

Отправщиков было два — свой в `analytics.ts` и типизированный `track.ts`, и
события расходились по двум потокам. Остался один.

Что снимается теперь: `cta_click` с местом и типом кнопки, `scroll_depth` по
секциям **и по процентам 25/50/75/100**, `view_hero`, `pricing_viewed` и
`lead_submitted` — последнее на переходе в бота, дальше человек уже вне сайта.
Проверено в браузере: события уходят.

**Счётчик можно подключить, не трогая код:** `site.analytics.gtmId`. Пока поле
пустое, на страницу не попадает ни одного стороннего скрипта и никакие данные
никуда не уходят. Подключается контейнер GTM, а не конкретный счётчик, — внутрь
ставится Метрика, GA4 или пиксель, и правок сайта это больше не потребует.
Идентификатор пропускается через проверку символов: кавычка в конфиге иначе
разорвала бы код.

### Разметка для поисковика

Добавлена схема `Organization` на всех страницах. Схема `FAQPage` была и раньше
— она отдаётся самим блоком вопросов, поэтому не может разойтись с тем, что
человек видит.

### Проверено

- `astro check` — 0 ошибок, 11 страниц;
- canonical и sitemap ведут на живой адрес;
- на каждой странице ровно две схемы: `Organization` и `FAQPage`;
- события `cta_click` и `scroll_depth` приходят в `dataLayer` — замером
  в браузере, а не по коду.

---

## Правки от 26 августа 2026 — три направления в продаже

Подписка обещала пять направлений, а наполнено по-настоящему было одно. Разложив
материалы заказчика по урокам (`docs/products/`), увидели картину точнее:

| Направление | Готовность | Почему |
| --- | --- | --- |
| Бег | курс целиком | Методичка 29 страниц, гайд 40 слайдов, книга |
| Сон | курс целиком | 13 страниц связного текста в исходнике |
| Массаж | курс целиком | 15 слайдов с прозой: виды, приёмы, подготовка |
| Самомассаж | половина | Есть тайминги и названия шести приёмов, нет описания самих приёмов |
| Зарядка | каркас | 11 слайдов из 13 — заголовок и открытые вопросы |

**«Самомассаж» и «Зарядка» переведены в статус готовящихся.** В подписке они не
заявляются: обещать в платном продукте то, что не открывается, — самый дорогой
способ получить возврат на первой же продаже.

Статус теперь про **продукт**, а не про страницу: лендинги обоих направлений
остались и доступны с главной отдельной ссылкой. Они продолжают собирать заявки
в бота, просто не входят в состав подписки. Раньше готовящееся направление было
принципиально некликабельным — тогда за ним и правда не было страницы.

На главной: «Открыто 3 из 11».

---

## Нужно от вас, чтобы включить продажи

1. **Ссылки оплаты LavaTop** — по одной на уровень →
   `site.subscription.tiers.base/premium/pro.payUrl`. Как только они появятся,
   кнопки на карточках включатся сами. Цены уже стоят: $9 / $20 / $100 в месяц.
3. **Домен** вместо `onrender.com` — для LavaTop не обязателен, но ссылка
   в рекламе и в боте должна выглядеть как ваша. Как появится — `PUBLIC_SITE_URL`
   в переменных окружения Render.
4. **Оферта и политика** → `site.links.offer`, `.privacy`. Без них платёжный
   сервис обычно не подключает.
5. **Боевая ссылка бота**: сейчас стоит `t.me/test_notify35_bot` — судя по
   названию, тестовый.
6. **Автор**: годы практики, число учеников, история и реальное фото — сейчас
   блок автора на страницах направлений не показывается вовсе, а на беговых
   стоит сток `mountain.jpg`.
   Отдельно подтвердите состав уровня «Сопровождение» за $100: еженедельный
   разбор видео, правка плана и личный чат — это обещания, которые придётся
   выполнять руками.
7. **Отзывы** — минимум 3, в формате «было → стало», с сегментом и по
   возможности с фото → `src/content/testimonials.ts`. Компонент готов и ждёт
   данных; пока их нет, секции на сайте не будет.
8. **Уроки зарядки** — чем ответить на вопросы её слайдов.
9. **База знаний бота по новым направлениям.** Сейчас бот подробно отвечает
   только по бегу, а бесплатная ступень — общий вход для всех пяти направлений.
   Пока это так, реклама сна или массажа приведёт человека к боту, которому
   нечего ему ответить.
10. **Идентификатор GTM** → `site.analytics.gtmId`, если нужна аналитика.
11. **Фотографии** — те 68 фото и видео из отчёта. Сейчас используются кадры,
   вырезанные из презентаций; у массажа исходник маленький (873×507) и на
   широком экране заметно мягкий.
