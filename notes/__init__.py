"""Личный бот заметок. Живёт в этом процессе, но не в этом проекте.

ЧТО ЭТО. Отдельный Telegram-бот с отдельным токеном: владелец шлёт ему
голосовое или пересылает чужое сообщение, бот расшифровывает и складывает
заметку в свой репозиторий. К Федерации здоровья отношения не имеет.

ПОЧЕМУ ТОГДА ЗДЕСЬ. Сервис уже оплачен, не засыпает и имеет два гигабайта
памяти — этого хватает на модель распознавания. Отдельный бесплатный инстанс
означал бы модель меньше, качество хуже и минуту ожидания после простоя.
Решение осознанное, а не случайное.

ГРАНИЦА, КОТОРУЮ НЕЛЬЗЯ СТИРАТЬ. Этот каталог **ничего не импортирует из
проекта** и **никем из проекта не импортируется**. Единственная точка
соприкосновения — три строки в `main.py`, подключающие маршрут. Всё
остальное — свои переменные окружения, свой токен, свой репозиторий.

Проверять это глазами не нужно, есть тест: `tests/test_notes.py`.

ЧТО БУДЕТ, ЕСЛИ МОДУЛЬ СЛОМАЕТСЯ. Ничего. Маршрут подключается только когда
задан токен, подключение обёрнуто в try, а обработка сообщения ловит любую
ошибку и отвечает владельцу текстом. Личные заметки не имеют права уронить
сервис, который принимает деньги.
"""

from .config import is_configured  # noqa: F401
from .router import router  # noqa: F401

__all__ = ["router", "is_configured"]
