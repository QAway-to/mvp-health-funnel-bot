import type { NavLink } from './types';

/**
 * Здесь остались только якоря — ссылки на разделы текущей страницы.
 *
 * Сквозные пункты («На главную», список направлений) шапка ставит сама:
 * они одинаковы везде, и передавать их с каждой страницы значило бы каждый
 * раз иметь шанс забыть. Именно так со страниц направлений и пропала любая
 * ссылка наружу.
 */

/** Якорная навигация одинакова на всех страницах направлений. */
export const nav: readonly NavLink[] = [
  { label: 'Программа', href: '#program' },
  { label: 'Цена', href: '#price' },
  { label: 'Вопросы', href: '#faq' },
];

/** Хаб: его собственные секции. Направления уже есть в меню шапки. */
export const hubNav: readonly NavLink[] = [
  { label: 'Доступ', href: '#price' },
];
